{
  "$schema": "http://json-schema.org/schema",
  "id": "ngx-hipster-prettier",
  "title": "Integrate Prettier into an angular CLI project",
  "type": "object",
  "properties": {
    "project": {
      "type": "string",
      "description": "Name of the project.",
      "$default": {
        "$source": "projectName"
      }
    }
  }
}
