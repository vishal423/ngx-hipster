{
  "$schema": "http://json-schema.org/schema",
  "id": "ngx-hipster-ng-add",
  "title": "NGX Hipster ng-add schematic",
  "type": "object",
  "properties": {
    "project": {
      "type": "string",
      "description": "Name of the project.",
      "$default": {
        "$source": "projectName"
      }
    },
    "configureNgx": {
      "type": "boolean",
      "description": "Configure project with ngx-hipster recommended configurations",
      "default": "no",
      "x-prompt": "Would you like to configure your project with ngx-hipster recommended configurations?"
    },
    "configureJest": {
      "type": "boolean",
      "description": "Configure project to use Jest library for unit testing",
      "default": "yes",
      "x-prompt": "Would you like to configure your application to use Jest for unit testing?"
    },
    "configureProxy": {
      "type": "boolean",
      "description": "Configure project with backend server proxy",
      "default": "yes",
      "x-prompt": "Would you like to configure your project to proxy backend calls?"
    }
  }
}
