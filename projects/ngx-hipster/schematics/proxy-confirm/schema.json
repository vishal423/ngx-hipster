{
  "$schema": "http://json-schema.org/schema",
  "id": "ngx-hipster-proxy",
  "title": "Configure proxy to backend server into an angular cli project",
  "type": "object",
  "properties": {
    "project": {
      "type": "string",
      "description": "Name of the project.",
      "$default": {
        "$source": "projectName"
      }
    },
    "configureProxy": {
      "type": "boolean",
      "description": "Configure project with backend server proxy",
      "default": "yes",
      "x-prompt": "Would you like to configure your project to proxy backend calls?"
    }
  }
}
